import { Request, Response } from 'express'
import Pedido from '../models/pedido'

export const updatePedidoService = async (req: Request, res: Response) => {
  // Implemente lógica aquí por favor. Es necesario realmente?
}
