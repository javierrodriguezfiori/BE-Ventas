import { Request, Response } from 'express'

// Implementar: pegarle a la banca y al put de usuario para actualizar el saldo
export const transferenciaService = async (req: Request, res: Response) => {
  return res.status(200).send()
}

